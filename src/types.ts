export type ImageSource = {
    width: number;
    height: number;
}

export type Rectangle = {
    x: number;
    y: number;
    width: number;
    height: number;
}

export type Point = {
  x: number;
  y: number;
}